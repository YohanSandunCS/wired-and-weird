# MediRunner Backend Dependencies - Production Face Recognition

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
websockets>=12.0

# Face Recognition Dependencies (Production-ready)
face_recognition>=1.3.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# Note: face_recognition requires:
# - CMake (install via: pip install cmake)
# - dlib (will be installed automatically with face_recognition)
# - Visual Studio Build Tools on Windows (for compilation)
#
# Install order:
# 1. pip install cmake
# 2. pip install -r requirements_production.txt
